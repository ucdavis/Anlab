import * as React from 'react';
import * as ReactDOM from 'react-dom';
import AdminOrderForm from './components/AdminOrderForm';

ReactDOM.render(
    <AdminOrderForm />,
    document.getElementById('react-app-admin')
);
