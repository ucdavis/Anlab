@model SystemAlert

@{
    ViewData["Title"] = "Home Page";
}

<div class="col-8">
    <div class="alert @(Model.Danger ? "alert-danger" : "alert-info")">
        <button type="button" class="close" data-dismiss="alert">×</button>
        @Html.Raw(Model.Content)
    </div>
</div>
