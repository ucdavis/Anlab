@model AnlabMvc.Models.Order.OrderReviewModel

@{
    ViewData["Title"] = "Pay with UC Davis Account";
}

<h2>Details</h2>


@Html.Partial("_OrderDetails")


<form asp-controller="Payment" asp-action="PayInternal" asp-route-id="@Model.Order.ShareIdentifier" method="post">
    <div class="form-group">
        <strong>Override Existing Account:</strong><span> (@Model.OrderDetails.Payment.Account)</span>
        <span><input type="text" class="form-control" name="overrideAccount" /></span>
    </div>
    <button type="submit" class="btn btn-primary"><i class="fa fa-money" aria-hidden="true"> @(String.Format("{0:C}", Model.OrderDetails.GrandTotal)) Pay Now</i></button> 
</form>



@section AdditionalStyles
{
    @{await Html.RenderPartialAsync("_DataTableStylePartial"); }
}

@section Scripts
{
    @{await Html.RenderPartialAsync("_DataTableScriptsPartial"); }

    <script type="text/javascript">
        $(function() {
            $("#table").dataTable();
        });
    </script>   

    @{await Html.RenderPartialAsync("_ShowdownScriptsPartial"); }
}

