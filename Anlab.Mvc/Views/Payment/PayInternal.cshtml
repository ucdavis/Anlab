@model AnlabMvc.Models.Order.OrderPaymentModel

@{
    ViewData["Title"] = "Pay with UC Davis Account";
}

<h2>Details</h2>


@Html.Partial("_OrderDetails", Model.OrderReviewModel)


<form asp-controller="Payment" asp-action="PayInternal" asp-route-id="@Model.OrderReviewModel.Order.ShareIdentifier" method="post">
    <div class="form-group">
        <strong>Override Existing Account:</strong><span> (@Model.OrderReviewModel.OrderDetails.Payment.Account)</span>
        <span><input type="text" class="form-control" name="overrideAccount" /></span>
    </div>
    <button type="submit" class="btn btn-primary"><i class="fa fa-money" aria-hidden="true"> @(String.Format("{0:C}", Model.OrderReviewModel.OrderDetails.GrandTotal)) Pay Now</i></button> 
</form>



@section AdditionalStyles
{
    @{await Html.RenderPartialAsync("_DataTableStylePartial"); }
}

@section Scripts
{
    @{await Html.RenderPartialAsync("_DataTableScriptsPartial"); }

    <script type="text/javascript">
        $(function() {
            $("#table").dataTable();
        });
    </script>   

    @{await Html.RenderPartialAsync("_ShowdownScriptsPartial"); }
}

